# Niyora — work board (2026-08-13)

Entry point for the new session. This is the **topic list**; the *detail* (locked consent copy, full reflection-redesign plan, commit history) lives in **`docs/handover-2026-08-12.md`** — read that for specifics. All work is on branch `build/testflight-1.0.0-b20` (pushed to origin), tsc clean, 729 tests.

**Status in one line:** Gate #1 (API key out of bundle) is DONE and live on TestFlight build 33; AI works via Firebase AI Logic + App Check. Now: finish the Moon reflection redesign, build the consent screen, and clear the App Store prep.

---

## 🔴 Tier 1 — Launch blockers (needed before App Store submission)

- [ ] **AI consent screen** (Apple 5.1.2 third-party-AI rule). Build the "Meet Moon" page — copy + behavior are LOCKED in the 08-12 doc (the ✓/✗ table + "I agree" + ✕, reuse the `moon_intro` onboarding page, gate the Talk-to-Moon entry, re-ask if skipped).
- [ ] **Privacy policy live** — page is built at `niyora.com/privacy` (Astro site, branch `feat/cosmic-theme`, NOT deployed). *Neha decides first:* canonical email (`neha@luminik.io` vs niyora.com) + min age (policy says 16, /terms says 13 — must match), then deploy.
- [ ] **Screenshots** (Neha) + **fill App Store Connect** from `docs/app-store-submission-pack.md`.
- [ ] **App Privacy answers** — decide: Health & Fitness collection? Device ID? (pack recommends Yes.) Territories: US-only for v1 recommended.

## 🟠 Tier 2 — Moon reflection redesign (the main build work)

- [ ] **Remove the acknowledge/echo beat** → the feeling guess becomes the opener. (Decided, not started. Touches moment-flow.ts, moment.tsx, moment-gemini.ts, tests.)
- [ ] **Prompt pass** — the 10 changes from the brainstorm (08-12 doc, "Reflection redesign plan"): vary the hedge framing, **merge ~15 lenses → 6-8 sharp ones**, add few-shot examples to weak cards (reflect_rule, reflect_friend), factsort reframe, act_help "help her be heard without burning bridges", reflect_chat always-points, verify the empty "nothing fits" state.
- [ ] **Wire the feedback loop into the AI** — the heart/cross steering model (like → deeper along that direction with NEW points; nothing → different lenses; dislike → drop that lens; draft → use liked / avoid disliked). *Plumbing:* track reactions BY LENS (currently by text only).
- [ ] **Add-context must re-reflect on the FULL picture** (original + addition), richer, not a tonal tweak.
- [ ] **Neha verifies the reactions visually** on device (icons / spacing / motion) — needs build 34.

## 🟡 Tier 3 — Crisis safety

- [ ] **Harm-to-others path** — narrow, high-bar detection of *credible/specific* intent to harm a named person (never venting) → de-escalation + resource, never coach a confrontation. (New gap from the brainstorm.)

## 🟢 Tier 4 — Security housekeeping (quick, do early)

- [ ] **Revoke the old Gemini key** at aistudio.google.com/apikey (real secret, leaked in builds ≤28, already pulled from EAS). *Neha.*
- [ ] **Restrict the Firebase plist API key** (the GitHub "secrets detected" alert). NOT a real secret — it's a Firebase client id designed to ship in every app — but restrict it in Google Cloud Console → Credentials to iOS bundle `com.niyora.app` + only the Firebase APIs used, then mark the GitHub alert resolved. App Check already limits abuse.

## ⚪ Tier 5 — Cleanup / small decisions

- [ ] **Delete `proxy/`** — dead abandoned backend, superseded by Firebase (left uncommitted).
- [ ] **Break-over notification** is now dead code (its flow was deleted) — delete the 3 symbols in `notifications.ts` or wire a break into Moon.
- [ ] **Ship build 34** once the reflection work + consent screen land — bundles everything for a real test round.

---

## Neha's manual steps (blocking some of the above)
1. Revoke old Gemini key (aistudio) · 2. Restrict Firebase key + resolve GitHub alert · 3. Decide privacy email + min age → deploy privacy page · 4. Screenshots + App Store Connect · 5. App Privacy + territory decisions.

## Suggested order for the new session
1. Quick security (revoke key, restrict Firebase key) — 5 min, clears the GitHub alert.
2. Acknowledge removal (clean, decided, unblocks the reflection flow).
3. Consent screen (launch blocker, copy locked).
4. Prompt pass + feedback wiring (the big one — do with Neha).
5. Build 34 → test → App Store prep in parallel.
